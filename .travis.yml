sudo: required
services:
  - docker
before_install:
  - docker build -t anandahlawat/docker-react -f Dockerfile.dev .
script:
  - docker run anandahlawat/docker-react npm run test -- -- coverage
    
